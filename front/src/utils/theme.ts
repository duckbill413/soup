'use client'

import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  typography: {
    fontFamily: 'inherit',
  },
  palette: {
    primary: {
      main: '#349B00',
    },
    secondary: {
      main: '#797979',
    },
  },
})

export default theme
